PHP Learning
1. Syntax ของ PHP คือ <?php ?>
2. Comment line ของ PHP คือ // หรือ #
3. Multi Comment Lines ของ PHP คือ /* */
4. การแสดงผลลัพธ์หรือ output คือการใช้ function echo หรือ print()
5. ความต่างของ echo กับ print() คือ print มีการส่งค่ากลับมาเพื่อคำนวณ(return) echo ไม่มีการส่งค่าคืนมีหน้าที่แค่แสดงผลลัพธ์(void) / print()ระบุได้ค่าเดียว แต่echo มีได้หลายตัว / echo เร็วกว่า print()
6. ตัวแปร Variables ใช้สำหรับเก็บข้อมูลในหน่วยความจำเพื่อใช้ในการประมวลผล เช่น ตัวเลข จัวอักษร หรือชุดข้อความ หรือกลุ่มข้อมูล (Array)
7. การนิยามตัวแปร $ชื่อตัวแปร = ค่าของตัวแปร
8. กฎการตั้งชื่อตัวแปร
    สามารถเป็นได้ทั้งตัวอักษร ตัวเลข สัญลักษณ์ _(underscores) และ $(dollar sign)
    อักษรตัวแรกห้ามเป็นตัวเลข
    Case Sensitive ตัวพิมพ์เล็ก-พิมพ์ใหญ่ มีความแตกต่างกัน
    ไม่ซ้ำกับ built-in function (ฟังก์ชั่นมาตรฐานใน PHP)
    ตัวแปรขึ้นต้นด้วย $(dollar sign)
    ตัวแปรไม่สามารถเว้นว่างได้ หรือเคาะเว้นวรรคได้
9. ชนิดข้อมูลของตัวแปร
    Integer คือเลขจำนวนเต็ม เช่น 1, 2, 3... (ฐาน 10, 8, 16) เป็นต้น
    Float/Double คือเลขจำนวนจริงเช่น 1.23, 3.14,... เป็นต้น
    Boolean คือข้อมูลทางตรรกศาสตร์มีค่า จริง(True), เท็จ(False)
    String คือตัวอักษรหรือชุดข้อความเขียนภายในเครื่องหมาย ""
    Array คือชุดข้อมูลหรือกลุ่มของข้อมูลที่มีชนิดข้อมูลเดียวกัน
    Object คือการกำหนดให้ตัวแปรนั้นเก็บคุณสมบัติของ Object (Attribute & Method) โดยการประกาศใช้งานผ่าน Class (OOP)
10. แสดงชนิข้อมูลของตัวแปร gettype(), settype()
11. Type Casting การเปลี่ยนชนิดของข้อมูลให้ชนิดที่ต้องการ โดยใส่ชนิดข้อมูลของตัวแปรไว้ในวงเล็บหน้าตัวแปรที่ต้องการจะเปลี่ยนชนิดของข้อมูล
12. ค่าคงที่ (Constant) การสร้างตัวแปรที่เก็บข้อมูลที่ไม่สามารถเปลี่ยนแปลงค่าได้
13. ฟังก์ชั่นเกี่ยวกับตัวแปร
    is_null คือฟังก์ชั่นสำหรับตรวจสอบว่าตัวแปรมีการกำหนดค่าไม่ ถ้ากำหนดจะมีค่าเป็น True (1) ถ้าไม่กำหนดจะมีค่าเป็น False
    unset ยกเลิกตัวแปรและคืนค่าให้หน่วยความจำ
    is_null ฟังก์ชั่นสำหรับตรวจสอบว่ามีค่าว่างหรือเลขศูนย์หรือไม่ ถ้าเป็นค่าว่างจะเป็น True(1) ถ้าไม่เป็นค่าว่างจะเป็น False
    is_null ฟังก์ชั่นสำหรับตรวจสอบว่าตัวแปรมีค่าว่างหรือไม่
    print_r() ฟังก์ชั่นสำหรับแสดงค่าตัวแปร Array
    var_dump() แสดงรายละเอียดตัวแปร
14. ตัวแปร Super Global
    $GLOBALS เป็นการประกาศให้เป็นตัวแปร global เพื่อให้ทุกส่วนสามารถเรียกใช้งานได้เลย
    $_SERVER เก็บค่าต่างๆของ web server ที่กำลังทำงานอยู่
    $_GET เป็นตัวแปรแบบ Array ใช้เก็บค่าที่ส่งมากับ URL
    $_POST ใช้เก็บค่าที่ส่งมากับ Form แบบ post method
    $ENV ตัวแปรที่จัดเก็บสภาพแวดล้อมทั่วไปและค่าต่างๆของ SERVER
    $_SESSION เก็บตัวแปร session
    $_COOKIE เก็บตัวแปร cookie
15. ตัวดำเนินการ Operator
    ตัวดำเนินการ Operator
    ตัวถูกดำเนินการ Operand
16. ตัวดำเนินการทางคณิตศาสตร์ + - * / %
17. ตัวดำเนินการเปรียบเทียบ
    == เท่ากับ ค่าเท่ากัน
    === เหมือนกัน ค่าเท่ากันและชนิดเดียวกัน
    != ไม่เท่ากับ ค่าไม่เท่ากัน
    <> ไม่เท่ากับ 
    !=== ไม่เหมือนกัน ค่าไม่เท่ากันและคนละชนิดกัน
    > มากกว่า
    < น้อยกว่า
    >= มากว่าเท่ากับ
    <= น้อยกว่าเท่ากับ
    <=> spaceship (php7)
18. ตัวดำเนินการกับข้อความ (String)
    .(concatenation) การต่อข้อความ
    .= ต่อข้อความและกำหนดค่า
19. ตัวดำเนินการเพิ่มค่า - ลดค่า
    ++(Prefix) เพิ่มค่าให้ a ก่อน 1 ค่าแล้วนำไปใช้
    ++(Postfix) นำค่าปัจจุบันใน a ไปใช้ก่อนแล้วค่อยเพิ่มค่า
    --(Prefix) ลดค่าให้ a ก่อน 1 ค่าแล้วนำไปใช้
    --(Postfix) นำค่าปัจจุบันใน a ไปใช้ก่อนแล้วค่อยลดค่า
20. Compound Assignment +=, -=, *=, /=, %=
21. ลำดับความสำคัญของตัวดำเนินการ
22. โครงสร้างควบคุม Control Structure แบบลำดับ Sequence, แบบมีเงื่อนไข Condition, แบบทำซ้ำ Loop
23. แบบมีเงื่อนไข Condition กลุ่มคำสั่งที่ใช้ตัดสินใจในการเลือกเงื่อนไขต่างๆภายในโปรแกรมมาทมำงาน if, Switch..Case
รูปแบบคำสั่งเงื่อนไขเดียว if statement เป็นคำสั่งที่ใช้กำหนดเงื่อนไขในการตัดสินใจทำงานของโปรแกรม ถ้าเงื่อนไขเป็นจริงจะทำตามคำสั่งๆที่กำหนดภายใต้เงื่อนไขนั้นๆ
    if(เงื่อนไข Condition){
        คำสั่งเมื่อเงื่อนไขเป็นจริง;
    }
รูปแบบคำสั่งแบบ2เงื่อนไข
    if(เงื่อนไข Condition){
        คำสั่งเมื่อเงื่อนไขเป็นจริง;
    }else{
        คำสั่งเมื่อเงื่อนไขเป็นเท็จ;
    }
รูปแบบคำสั่งแบบหลายเงื่อนไข
    if(เงื่อนไขที่ 1){
        คำสั่งเมื่อเงื่อนไขที่ 1 เป็นจริง;
    }elseif(เงื่อนไขที่ 2){
        คำสั่งเมื่อเงื่อนไขที่ 2 เป็นจริง;
    }elseif(เงื่อนไขที่ 3){
        คำสั่งเมื่อเงื่อนไขที่ 3 เป็นจริง;
    }else{
        คำสั่งเมื่อทุกเงื่อนไขเป็นเท็จ;
    }
24.ตัวดำเนินการทางตรรกศาสตร์
    && AND
    || OR
    ! NOT
    and เหมือนกับ && แต่ลำดับความสำคัญน้อยกว่า
    or เหมือนกับ || แต่ลำดับความสำคัญน้อยกว่า
    not เหมือนกับ ! แต่ลำดับความสำคัญน้อยกว่า
25. if..else แบบลดรูป (Ternary Operator)
    (เงื่อนไข)? คำสั่งเมื่อเงื่อนไขเป็นจริง : คำสั่งเมื่อเงื่อนไขเป็นเท็จ
        if(เงื่อนไข){
            เงื่อนไขเป็นจริง
        }else{
            เงื่อนไขเป็นเท็จ
        }
26. Switch..Case
Switch เป็นคำสั่งที่ใช้กำหนดเงื่อไขคล้ายๆกับ if แต่จะเลือกเพียงหนึ่งทางเลือกออกมาทำงานโดยนำค่าในตัวแปรมากำหนดเป็นทางเลือกผ่านคำสั่ง case
รูปแบบคำสั่ง
switch(สิ่งที่ต้องการตรวจสอบ) {
    case ค่าที่ 1: คำสั่งที่1;
        break;
    case ค่าที่ 2: คำสั่งที่2;
        break;
        ...
    case ค่าที่ N: คำสั่งที่N;
        break;
    
    default:คำสั่งเมื่อไม่มีค่าที่ตรงกับที่ระบุใน case
}
***คำสั่ง break
จะทำให้โปรแกรมกระโดดออกไปทำงานนอกคำสั่ง switch ถ้าไม่มีคำสั่ง break โปรแกรมจะทำคำสั่งต่อไปเรื่อยๆจนจบการทำงาน
27. While Loop กลุ่มคำสั่งที่ใช้ในการวนรอบ (Loop) โปรแกรมจะทำงานไปเรื่อยๆจบกว่าเงื่อนไขที่กำหนดไว้จะเป็นเท็จ จึงจะหยุดทำงาน
While Loop
จะทำงานตามคำสั่งภายใน while ไปเรื่อยๆเมื่อเงื่อไขที่กำหนดเป็นจริง
    while(เงื่อนไข){
        คำสั่งที่จะทำซ้ำเมื่อเงื่อนไขเป็นจริง;
    }
28. For Loop เป็นรูปแบบที่ใช้ในการตรวจสอบเงื่อนไข มีการกำหนดค่าเริ่มต้นและเปลี่ยนค่าไปพร้อมๆกัน เมื่อเงื่อนไขในคำสั่ง for เป็นจริงก็จะทำงานตามคำสั่งที่แสดงไว้ภายใต้คำสั่ง for ไปเรื่อยๆ
    for(ค่าเริ่มต้นของตัวแปร; เงื่อนไข; เปลี่ยนแปลงค่าตัวแปร){
        คำสั่งเมื่อเงื่อนไขเป็นจริง;
    }
29. Do..While โปรแกรมจะทำงานจามคำสั่งอย่างน้อย 1 รอบ เมื่อทำงานเสร็จจะมาตรวจสอบเงื่อนไขที่คำสั่ง while
ถ้าเงื่อนไขเป็นจริงจะวนกลับขึ้นไปทำงานที่คำสั่งใหม่อีกรอบ แต่ถ้าเป็นเท็จจะหลุดออกจากลูป
    do{
        คำสั่งต่างๆ เมื่อเงื่อนไขเป็นจริง;
    }while(เงื่อนไข);
30. ข้อแตกต่างและการใช้งาน Loop
    For ใช้ในกรณีรู้จำนวนรอบที่ชัดเจน
    While ใช้ในกรณีที่ไม่รู้จำนวนรอบ
    Do..while ใช้ในกรณีที่อยากให้ลองทำก่อน 1 รอบแล้วทำซ้ำไปเรื่อยๆทราบเท่าที่เงื่อนไขเป็นจริง
31. คำสั่งที่เกี่ยวข้องกับ Loop
    break ถ้าโปรแกรมพบคำสั่งนี้จะหลุดจากการทำงานลูปทันที เพื่อไปทำคำสั่งอื่นที่อยู่นอกลูป
    continue คำสั่งนี้จะทำให้หยุดการทำงานแล้วย้อนกลับไปเริ่มต้นการทำงานที่ต้นลูปใหม่
    exit คำสั่งให้โปรแกรมหยุดทำงาน (ใช้ในกรณีที่มีข้อผิดพลาดเกิดขึ้่นในโปรแกรม จะออกจากการทำงานของโปรแกรมทั้งหมด)
32. ฟังก์ชั่นคืออะไร
    ความหมายที่1: ชุดคำสั่งที่นำมาเขียนรวมกันเป็นกลุ่มเพื่อให้เรียกใช้งานตามวัตถุประสงค์ที่ต้องการและลดความซ้ำซ้อนของคำสั่งที่ใช้งานบ่อยๆ ฟังก์ชั่นสามารถนำไปใช้งานได้ทุกที่และแก้ไขได้ในภายหลัง ทำให้โค๊ดในโปรแกรมมีระเบียบและใช้งานได้สะดวกมากยิ่งขึ้น
    ความหมายที่2: โปรแกรมย่อยที่นำเข้ามาเป็นส่วนหนึ่งของโปรแกรมหลัก เพื่อให้สามารถเรียกใช้งานได้โดยไม่จำเป็นต้องเขียนโค๊ดคำสั่งใหม่ทั้งหมด
33. ชนิดของฟังก์ชั่น
    ฟังก์ชั่นมาตรฐาน (Built-In Function) คือ ฟังก์ชั่นที่มีอยู่ในภาษา PHP ผู้ใช้สามารถเรียกใช้งานได้เลย
    ฟังก์ชั่นที่ผู้ใช้สร้างขึ้นมาเอง (User-Define Function) คือ ฟังก์ชั่นที่ถูกสร้างขึ้นมาเพื่อวัตถุประสงค์ให้ทำงานตามที่ผู้ใช้ต้องการ
34. กฎการตั้งชื่อฟังก์ชั่น
    ชื่อฟังก์ชั่นต้องไม่ซ้ำกัน
    ชื่อฟังก์ชั่นสามารถเป็นตัวอักษร ตัวเลข หรือขีดเส้นล่าง(underscore)
    ชื่อของฟังก์ชั่นต้องไม่ขึ้นต้นด้วยตัวเลข
35. รูปแบบของฟังก์ชั่น
    1.ฟังก์ชั่นที่ไม่มีการรับค่าและส่งค่า
    function ชื่อฟังก์ชั่น(){
        //คำสั่งต่างๆ
    }
    การเรียกใช้ฟังก์ชั่น
    ชื่อฟังก์ชั่น();
    2.ฟังก์ชั่นที่มีการรับค่าเข้ามาทำงาน
    function ชื่อฟังก์ชั่น(parameter1,parameter2,...){
        //กลุ่มคำสั่งต่างๆ
    }
    อาร์กิวเมนต์ คือ ตัวแปรหรือค่าที่ต้องการส่งมาให้กับฟังก์ชั่น (ตัวแปรส่ง)
    พารามิเตอร์ คือ ฟตัวแปรที่ฟังก์ชั่นสร้างไว้สำหรับค่าที่จะเข้ามาให้กับฟังก์ชั่น (ตัวแปรรับ)
    การเรียกใช้งาน(argument1,argument2,...);
    3.ฟังก์ชั่นที่มีส่งค่าออกมา
    function ชื่อฟังก์ชั่น(){
        return ค่าที่จะส่งออกไป
    }
    4. ฟังก์ชั่นที่มีการรับค่าเข้ามาและส่งค่าออกไป
    function ชื่อฟังก์ชั่น(parameter1, parameter2,...){
        return ค่าที่่ส่งออกไป
    }
36. ฟังก์ชั่นแบบกำหนดค่าเริ่มต้น
    function ชื่อฟังก์ชั่น (name = "kaveekit",parameter2,...){
        //คำสั่งต่างๆ
    }
37. ขอบเขตตัวแปร
    Local Variable ตัวแปรที่ทำงานอยู่ในฟังก์ชั่นมีขอบเขตการทำงานตั้งแต่จุดเริ่มต้นไปจนสิ้นสุดของฟังก์ชั่น
    Global Variable ตัวแปรที่ทำงานอยู่นอกฟังก์ชั่นมีขอบเขตการทำงานตั้งแต่จุดเริ่มต้นไปจนถึงจุดสิ้นสุดของไฟล์ที่ประกาศใช้
38. Include & Require
    include() ฟังก์ชั่นสำหรับโหลดไฟล์เข้ามาทำงาน โดยจะทำงานตั้งแต่เริ่มต้นจนจบโปรแกรม ถ้าหาไฟล์ไม่เจอจะแจ้งเตือนข้อผิดพลาด(warning)แล้วข้ามไปทำงานส่วนอื่นๆต่อ
    include_one() ทำงานเหมือนฟังก์ชั่น include แต่จะเรียกใช้งานไฟล์แค่ครั้งเดียว
    require() ฟังก์ชั่นสำหรับโหลดไฟล์เข้ามาทำงาน โดยจะทำงานตั้งแต่เริ่มต้นจนจบโปรแกรม ถ้าหาไฟล์ไม่เจอจะแจ้งเตือนข้อผิดพลาดและหยุดทำงานโปรแกรม
    require_one() ทำงานเหมือนฟังก์ชั่น require แต่จะเรียกใช้งานไฟล์แค่ครั้งเดียว
39. อาร์เรย์ Array
    ข้อจำกัดของชนิดข้อมูลพื้นฐาน การประกาศตัวแปรแต่ละครั้ง ตัวแปร 1 ตัวสามารถเก็บข้อมูลได้แค่ 1 ค่าเท่านั้น เช่น $number = 1; ถ้าอยากเก็บเลข 10 ค่าต้องทำอย่างไร ?
    ต้องประกาศตัวแปร 10 ตัวแปรหรือไม่ ?
    Array คือ ความหมายที่1 ชุดของตัวแปรที่อยู่ในรูปลำดกับใช้เก็บค่าข้อมูลให้อยู่ในกลุ่มเดียวกัน ข้อมูลภายในอาร์เรย์จะถูกเก็บบนหน่วยความจำในตำแหน่งที่ต่อเนื่องกัน โดยขนาดของอาร์เรย์จะเล็กหรือใหญ่ขึ้นอยู่กับจำนวนมิติที่กำหนดขึ้น
    ความหมายที่2 เป็นตัวแปรที่ใช้ในการเก็บข้อมูลที่มีลำดับที่ต่อเนื่อง ซึ่งข้อมูลมีค่าได้หลายค่าโดยใช้ชื่ออ้างอิงได้เพียงชื่อเดียวและใช้หมายเลขกำกับ(index) ให้กับตัวแปรเพื่อจำแนกความต่างของค่าตัวแปรแต่ละตัว
    โครงสร้างของ Array
    1. ข้อมูลที่อยู่ในอาร์เรย์จะเรียกว่าสมาชิก หรือ อิลิเมนต์(element)
    2. แต่ละอิลิเมนต์ (element) จะเก็บค่าข้อมูล (value) และอินเด็กซ์ (Index) เอาไว้
    3. Index หมายถึงคัย์ของอาร์เรย์ใช้อ้างอิงตำแหน่งของ element
    4. Index เป็นได้ทั้งตัวเลขหรือตัวอักษร (keys)
    5. สมาชิกใน array ต้องมีชนิดข้อมูลเหมือนกัน
    6. สมาชิกใน array จะถูกคั่นด้วยเครื่องหมาย comma
    ประเภทของ Array
    1. Array แบบเดี่ยว
    2. Array แบบจับคู่ (Associate)
    รูปแบบการสร้าง Array
    1. ใช้ฟังก์ชั่น array
    2. ใช้ฟังก์ชั่น range
    3. ใช้สัญลักษณ์กล้ามปู[]
40. การสร้าง Array ด้วยฟังก์ชั่น Range
    ใช้สร้าง array แบบระบุช่วงตัวเลขหรือตัวอักษร เรียงจากน้อยไปมาก
    - range(start,stop,step);
    - $number=range(4,8); ข้อมูลอยู่ในช่วง 4-8
    - $number=range(0,20,5); ข้อมูลอยู่ในช่วง 0-20 ค่าความต่างคือ 5
    - $alphabet=range(A,F); ข้อมูลอยู่ในช่วง A-F
    - $alphabet=range(A,F,2); ข้อมูลอยู่ในช่วง A-F ค่าความต่างคือ 2
41. การสร้าง Array ด้วย []
    $number = [1,2,3,4]; ได้เลย
42. เข้าถึง Array ด้วยการ Loop
    count($pets); นับจำนวนสมาชิกในอาร์เรย์ ใช้กับอาร์เรย์ที่มี index เป็นตัวเลขที่เรียงลำดับจากน้อยไปมาก
    array_count_values($pets); ใช้นับความถี่ของข้อมูลใน array ที่มีค่าซ้ำกัน
43. //! เข้าถึง Array ด้วยการ While Loop
    ใช้ฟังก์ชั่น each ในการอ่านค่า array ทั้งข้อมูลและอินเด็กซ์ function each ถูก PHP เอาออกไปตั้งแต่ version 8.0 ให้ใช้ foreach แทน
    foreach ($index as $key => $value) {
    print($key);
    }
44. //! การเข้าถึงสมาชิกด้วยฟังก์ชั่น list
    ใช้ฟังก์ชั่น list ในการอ่านค่า array ทั้งข้อมูลและออินเด็กซ์และเก็บลงในตัวแปร 2 ตัวแปรโดยการเก็บแยกเป็นข้อมูลและอินเด็กซ์ลงในตัวแปรเหล่านั้น //! ใช้ foreach
45. การเข้าถึงข้อมูลด้วย foreach แบบธรรมดา 
    foreach($array as $value)
        statement
        